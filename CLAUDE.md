Read DESIGN.md before writing any code — it is the authoritative spec (settled decisions, tool signatures, state machine, on-disk formats).
When you resolve an item in DESIGN.md §18 (OPEN questions), record the decision there.
