"""Read-only observer over a session directory (DESIGN §16).

`altf watch <dir>` / `python -m altf.watch <dir>` — needs the `altf[watch]`
extra (Textual). The replay helpers in `replay.py` are dependency-free.
"""
